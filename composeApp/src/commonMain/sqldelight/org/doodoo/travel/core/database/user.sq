   CREATE TABLE user (
       id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
       name TEXT NOT NULL,
       interest TEXT NOT NULL
   );

   selectAllUsers:
   SELECT * FROM user;

   insertUser:
   INSERT INTO user (name, interest) VALUES ( ?, ?);